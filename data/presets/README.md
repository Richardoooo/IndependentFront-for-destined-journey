# 预设目录

此处存放 SillyTavern 兼容的 AI Response Preset JSON 文件。

预设导入自 SillyTavern 的 AI Response Configuration，包含：
- 采样器参数（Temperature, TopP, MaxTokens 等）
- 子提示词条目列表（prompts[]）

预设文件通过设置页 → Agent 配置 → 预设系统进行管理。

（当前为空，后续从 SillyTavern 导出或手动创建）
